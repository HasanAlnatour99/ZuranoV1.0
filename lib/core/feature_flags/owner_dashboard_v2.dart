/// When **true**, the owner **Overview** tab uses snapshot-backed dashboard UI.
///
/// Set to **false** to use the legacy overview (KPI grid, revenue chart, insights)
/// without waiting for dashboard snapshots.
const bool kOwnerDashboardV2Enabled = false;
