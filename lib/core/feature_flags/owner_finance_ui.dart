/// Owner **Finance** tab UI selection.
///
/// When **true**, uses legacy [OwnerMoneyModule]: period chips (today / month),
/// operational vs cash recognition, [LuxuryKpiCard] totals, and stream-backed
/// sales / payroll / expenses sections.
///
/// When **false**, uses [MoneyDashboardModule]: month selector, trends, charts,
/// and aggregated money dashboard providers.
const bool kOwnerFinanceLegacyUiEnabled = false;
